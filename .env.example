# AgentPulse environment — copy to .env (which is gitignored) and fill in.
#
# Only needed for the AI-powered "Suggest next checks" triage (dashboard button,
# MCP get_next_check_steps, CLI --next). Everything else works without it.
ANTHROPIC_API_KEY=sk-ant-...

# Optional overrides:
# PORT=5001
