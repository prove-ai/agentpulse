# Dependencies are defined in pyproject.toml. For development, use:
#
#     pip install -e ".[all]"
#
# This file installs the package with every optional extra, for tooling that
# still expects a requirements.txt.
-e .[all]
